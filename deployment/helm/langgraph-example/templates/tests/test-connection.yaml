apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "langgraph-example.fullname" . }}-test-connection"
  labels:
    {{- include "langgraph-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "langgraph-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
