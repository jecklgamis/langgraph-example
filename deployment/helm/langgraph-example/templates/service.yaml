apiVersion: v1
kind: Service
metadata:
  name: {{ include "langgraph-example.fullname" . }}
  labels:
    {{- include "langgraph-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "langgraph-example.selectorLabels" . | nindent 4 }}
